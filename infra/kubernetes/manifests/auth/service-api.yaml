apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: go-auth
  labels:
    app: auth-api
    app.kubernetes.io/name: auth-api
    app.kubernetes.io/part-of: go-auth
spec:
  type: ClusterIP
  selector:
    app: auth-api
  ports:
    - name: http
      port: 5050
      targetPort: http
      protocol: TCP
