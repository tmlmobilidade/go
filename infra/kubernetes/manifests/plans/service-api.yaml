apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server api:5050"
  name: api
  namespace: go-plans
  labels:
    app: plans-api
    app.kubernetes.io/name: plans-api
    app.kubernetes.io/part-of: go-plans
spec:
  type: ClusterIP
  selector:
    app: plans-api
  ports:
    - name: http
      port: 5050
      targetPort: http
      protocol: TCP
