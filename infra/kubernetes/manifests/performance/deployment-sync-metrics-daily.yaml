apiVersion: apps/v1
kind: Deployment
metadata:
  name: performance-sync-metrics-daily
  namespace: go-performance
  labels:
    app: performance-sync-metrics-daily
    app.kubernetes.io/name: performance-sync-metrics-daily
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: go-performance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: performance-sync-metrics-daily
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  template:
    metadata:
      labels:
        app: performance-sync-metrics-daily
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: sync-metrics-daily
          image: ghcr.io/tmlmobilidade/go-performance-sync-metrics-daily:k8s
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: performance-secret
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
