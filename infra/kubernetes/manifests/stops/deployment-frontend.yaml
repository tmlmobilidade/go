apiVersion: apps/v1
kind: Deployment
metadata:
  name: stops-frontend
  namespace: go-stops
  labels:
    app: stops-frontend
    app.kubernetes.io/name: stops-frontend
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: go-stops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stops-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: stops-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [stops-frontend]
                topologyKey: kubernetes.io/hostname
      containers:
        - name: frontend
          image: ghcr.io/tmlmobilidade/go-stops-frontend:staging
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: stops-secret
          livenessProbe:
            httpGet:
              path: /stops
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /stops
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
