apiVersion: apps/v1
kind: Deployment
metadata:
  name: stops-api
  namespace: go-stops
  labels:
    app: stops-api
    app.kubernetes.io/name: stops-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: go-stops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stops-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: stops-api
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [stops-api]
                topologyKey: kubernetes.io/hostname
      containers:
        - name: api
          image: ghcr.io/tmlmobilidade/go-stops-api:staging
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 5050
              protocol: TCP
          envFrom:
            - secretRef:
                name: stops-secret
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
