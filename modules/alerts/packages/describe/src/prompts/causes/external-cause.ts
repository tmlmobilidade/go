/* * */

import { type I18nCode } from '@tmlmobilidade/go-types-shared';

/**
 * Prompt for the generation of the alert descriptions for external causes,
 * where the operator is not at fault, to be used as a closing statement.
 */
export const externalCauseClosingPrompt: Record<I18nCode, string> = {

	en: '',

	pt: 'No final da descrição, usa um fecho empático sem assumir culpa do operador, como "Agradecemos a sua compreensão." ou formulação equivalente.',

};
