apiVersion: apps/v1
kind: Deployment
metadata:
  name: replicator-stream
  namespace: go-replicator
  labels:
    app: replicator-stream
    app.kubernetes.io/name: replicator-stream
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: go-replicator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: replicator-stream
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  template:
    metadata:
      labels:
        app: replicator-stream
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: stream
          image: ghcr.io/tmlmobilidade/go-replicator-stream:staging
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: replicator-secret
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
