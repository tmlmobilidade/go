apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server frontend:3000"
  name: frontend
  namespace: go-controller
  labels:
    app: controller-frontend
    app.kubernetes.io/name: controller-frontend
    app.kubernetes.io/part-of: go-controller
spec:
  type: ClusterIP
  selector:
    app: controller-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
