name: tml-sae-auth-db

services:
    auth-db:
        image: mongo:latest
        ports:
            - 37001:27017
        environment:
            MONGO_INITDB_ROOT_USERNAME: root
            MONGO_INITDB_ROOT_PASSWORD: root
            MONGO_INITDB_DATABASE: production
        volumes:
            - ./mock.dump:/dump/db.dump
            - ./local-setup.sh:/docker-entrypoint-initdb.d/setup.sh
