'use client';

import { ColorInput as MantineColorInput, ColorInputProps as MantineColorInputProps } from '@mantine/core';

/* * */

export function ColorInput({ ...props }: MantineColorInputProps) {
	//

	//
	// A. Render components

	return (
		<MantineColorInput
			{...props}
		/>
	);

	//
}
