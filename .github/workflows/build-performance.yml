name: 'Performance Explorer | Build & Push'

on:
  workflow_dispatch:
  push:
    paths:
      - 'performance/**'
    branches:
      - production
      - staging

# # #

jobs:

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  # frontend:
  #   runs-on: ubuntu-latest
  #   environment: ${{ github.ref_name }}
  #   permissions:
  #     contents: read
  #     packages: write

  #   steps:

  #     - name: Checkout repository
  #       uses: actions/checkout@main

  #     - name: Cache turbo build setup
  #       uses: actions/cache@v4
  #       with:
  #         path: .turbo
  #         key: ${{ runner.os }}-turbo-${{ github.sha }}
  #         restore-keys: |
  #           ${{ runner.os }}-turbo-

  #     - name: Login to GitHub Container Registry
  #       uses: docker/login-action@master
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Build and Push Docker Image
  #       uses: docker/build-push-action@master
  #       with:
  #         push: true
  #         context: ./performance
  #         file: ./performance/apps/frontend/Dockerfile
  #         tags: ghcr.io/tmlmobilidade/sae-performance-frontend:${{ github.ref_name }}
  #         build-args: |
  #           ENVIRONMENT=${{ github.ref_name }}

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  # api:
  #   runs-on: ubuntu-latest
  #   environment: ${{ github.ref_name }}
  #   permissions:
  #     contents: read
  #     packages: write

  #   steps:

  #     - name: Checkout repository
  #       uses: actions/checkout@main

  #     - name: Cache turbo build setup
  #       uses: actions/cache@v4
  #       with:
  #         path: .turbo
  #         key: ${{ runner.os }}-turbo-${{ github.sha }}
  #         restore-keys: |
  #           ${{ runner.os }}-turbo-

  #     - name: Login to GitHub Container Registry
  #       uses: docker/login-action@master
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Build and Push Docker Image
  #       uses: docker/build-push-action@master
  #       with:
  #         push: true
  #         context: ./performance
  #         file: ./performance/apps/api/Dockerfile
  #         tags: ghcr.io/tmlmobilidade/sae-performance-api:${{ github.ref_name }}

  # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # #

  # nginx:
  #   runs-on: ubuntu-latest
  #   environment: ${{ github.ref_name }}
  #   permissions:
  #     contents: read
  #     packages: write

  #   steps:

  #     - name: Checkout repository
  #       uses: actions/checkout@main

  #     - name: Cache turbo build setup
  #       uses: actions/cache@v4
  #       with:
  #         path: .turbo
  #         key: ${{ runner.os }}-turbo-${{ github.sha }}
  #         restore-keys: |
  #           ${{ runner.os }}-turbo-

  #     - name: Login to GitHub Container Registry
  #       uses: docker/login-action@master
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Build and Push Docker Image
  #       uses: docker/build-push-action@master
  #       with:
  #         push: true
  #         context: ./performance/apps/nginx
  #         file: ./performance/apps/nginx/Dockerfile
  #         tags: ghcr.io/tmlmobilidade/sae-performance-nginx:${{ github.ref_name }}
  #         build-args: |
  #           NGINX_SERVER_NAME=${{ github.ref_name == 'staging' && 'staging.performance.sae.carrismetropolitana.pt' || 'performance.sae.carrismetropolitana.pt' }}

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  sync-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:

      - name: Checkout repository
        uses: actions/checkout@main

      - name: Cache turbo build setup
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Login to GitHub Container Registry
        uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@master
        with:
          push: true
          context: ./performance
          file: ./performance/apps/sync-metrics/Dockerfile
          tags: ghcr.io/tmlmobilidade/sae-performance-sync-metrics:${{ github.ref_name }}