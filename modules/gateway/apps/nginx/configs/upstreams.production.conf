# # #
# UPSTREAMS

# Alerts

upstream alerts_upstream {
	least_conn;
	zone alerts_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}

# Auth

upstream auth_upstream {
	least_conn;
	zone auth_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}

# Controller

upstream controller_upstream {
	least_conn;
	zone controller_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}

# Locations

upstream locations_upstream {
	least_conn;
	zone locations_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}

# Performance

upstream performance_upstream {
	least_conn;
	zone performance_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}

# Plans

upstream plans_upstream {
	least_conn;
	zone plans_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}

# Stops

upstream stops_upstream {
	least_conn;
	zone stops_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}

# Exports

upstream exports_upstream {
	least_conn;
	zone exports_upstream 64K;
	server 0.0.0.0 max_fails=30 fail_timeout=60s;
	keepalive 10;
}