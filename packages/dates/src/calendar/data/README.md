# Operational Date Snapshot

`operational-dates.json` is a temporary static snapshot of
`https://go.carrismetropolitana.pt/api/dates/public`, fetched on 2026-08-07.

The records are sorted by `date` to keep later snapshot updates reviewable.
Runtime consumers must access the data through `enrichOperationalDate`; they
should not import the JSON file directly.

