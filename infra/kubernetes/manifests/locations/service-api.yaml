apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server api:5050"
  name: api
  namespace: go-locations
  labels:
    app: locations-api
    app.kubernetes.io/name: locations-api
    app.kubernetes.io/part-of: go-locations
spec:
  type: ClusterIP
  selector:
    app: locations-api
  ports:
    - name: http
      port: 5050
      targetPort: http
      protocol: TCP
