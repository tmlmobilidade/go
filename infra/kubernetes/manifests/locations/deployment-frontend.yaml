apiVersion: apps/v1
kind: Deployment
metadata:
  name: locations-frontend
  namespace: go-locations
  labels:
    app: locations-frontend
    app.kubernetes.io/name: locations-frontend
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: go-locations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: locations-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: locations-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [locations-frontend]
                topologyKey: kubernetes.io/hostname
      containers:
        - name: frontend
          image: ghcr.io/tmlmobilidade/go-locations-frontend:k8s
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: locations-secret
          livenessProbe:
            httpGet:
              path: /locations
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /locations
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
