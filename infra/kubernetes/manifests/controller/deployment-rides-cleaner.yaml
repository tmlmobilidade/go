apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-rides-cleaner
  namespace: go-controller
  labels:
    app: controller-rides-cleaner
    app.kubernetes.io/name: controller-rides-cleaner
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: go-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: controller-rides-cleaner
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  template:
    metadata:
      labels:
        app: controller-rides-cleaner
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: rides-cleaner
          image: ghcr.io/tmlmobilidade/go-controller-rides-cleaner:k8s
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: controller-secret
          resources:
            requests:
              cpu: 50m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 2Gi
