apiVersion: v1
kind: Service
metadata:
  name: stops-nginx
  namespace: go-stops
  labels:
    app: stops-nginx
    app.kubernetes.io/name: stops-nginx
    app.kubernetes.io/part-of: go-stops
spec:
  type: NodePort
  selector:
    app: stops-nginx
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30083
      protocol: TCP
