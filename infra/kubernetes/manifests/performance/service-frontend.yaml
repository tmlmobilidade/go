apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server frontend:3000"
  name: frontend
  namespace: go-performance
  labels:
    app: performance-frontend
    app.kubernetes.io/name: performance-frontend
    app.kubernetes.io/part-of: go-performance
spec:
  type: ClusterIP
  selector:
    app: performance-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
