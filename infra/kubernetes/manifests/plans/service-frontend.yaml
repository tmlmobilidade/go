apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server frontend:3000"
  name: frontend
  namespace: go-plans
  labels:
    app: plans-frontend
    app.kubernetes.io/name: plans-frontend
    app.kubernetes.io/part-of: go-plans
spec:
  type: ClusterIP
  selector:
    app: plans-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
