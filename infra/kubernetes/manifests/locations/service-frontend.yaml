apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server frontend:3000"
  name: frontend
  namespace: go-locations
  labels:
    app: locations-frontend
    app.kubernetes.io/name: locations-frontend
    app.kubernetes.io/part-of: go-locations
spec:
  type: ClusterIP
  selector:
    app: locations-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
