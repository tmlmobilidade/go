apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server api:5050"
  name: api
  namespace: go-exporter
  labels:
    app: exporter-api
    app.kubernetes.io/name: exporter-api
    app.kubernetes.io/part-of: go-exporter
spec:
  type: ClusterIP
  selector:
    app: exporter-api
  ports:
    - name: http
      port: 5050
      targetPort: http
      protocol: TCP
