apiVersion: apps/v1
kind: Deployment
metadata:
  name: performance-frontend
  namespace: go-performance
  labels:
    app: performance-frontend
    app.kubernetes.io/name: performance-frontend
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: go-performance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: performance-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: performance-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [performance-frontend]
                topologyKey: kubernetes.io/hostname
      containers:
        - name: frontend
          image: ghcr.io/tmlmobilidade/go-performance-frontend:staging
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: performance-secret
          livenessProbe:
            httpGet:
              path: /performance
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /performance
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
