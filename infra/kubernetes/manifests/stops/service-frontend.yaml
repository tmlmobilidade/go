apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server frontend:3000"
  name: frontend
  namespace: go-stops
  labels:
    app: stops-frontend
    app.kubernetes.io/name: stops-frontend
    app.kubernetes.io/part-of: go-stops
spec:
  type: ClusterIP
  selector:
    app: stops-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
