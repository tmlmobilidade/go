apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-sams-feeder
  namespace: go-controller
  labels:
    app: controller-sams-feeder
    app.kubernetes.io/name: controller-sams-feeder
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: go-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: controller-sams-feeder
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  template:
    metadata:
      labels:
        app: controller-sams-feeder
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: sams-feeder
          image: ghcr.io/tmlmobilidade/go-controller-sams-feeder:k8s
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: controller-secret
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
