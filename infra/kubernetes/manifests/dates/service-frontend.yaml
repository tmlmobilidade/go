apiVersion: v1
kind: Service
metadata:
  # Name must match the upstream in the baked-in nginx config: "server frontend:3000"
  name: frontend
  namespace: go-dates
  labels:
    app: dates-frontend
    app.kubernetes.io/name: dates-frontend
    app.kubernetes.io/part-of: go-dates
spec:
  type: ClusterIP
  selector:
    app: dates-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
